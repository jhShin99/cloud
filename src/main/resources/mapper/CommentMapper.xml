<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.cloud.mapper.CommentMapper">

    <insert id="insertComment">
        INSERT INTO board_comment (board_id, user_id, content)
        VALUES (#{boardId}, #{userId}, #{content})
    </insert>

    <select id="selectCommentList" resultType="BoardComment">
        SELECT id, board_id, user_id, content, created_at
        FROM board_comment
        WHERE board_id = #{boardId}
        ORDER BY id ASC
    </select>

    <delete id="deleteComment">
        DELETE
        FROM board_comment
        WHERE id = #{id}
          AND user_id = #{userId}
    </delete>
</mapper>